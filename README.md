# oranges

## Usage
```bash
bash <(curl -fsSL 'https://raw.githubusercontent.com/mehrad-meraji/oranges/refs/heads/main/rind.sh')
```

**Note:** Use `bash` (not `sh`) to ensure proper terminal interaction for Bitwarden login.

